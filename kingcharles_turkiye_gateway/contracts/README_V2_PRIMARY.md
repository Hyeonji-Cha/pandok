# Contract policy

The authoritative Türkiye Gateway → AWS contract is the repository-root file:

`../../contracts/telemetry-event-v2.schema.json`

Do not create an independently edited v2 schema in this folder.

`aggregate-export-v1.schema.json` in this directory is retained only as an
optional secondary/reconciliation contract and is not the primary AWS-bound path.
